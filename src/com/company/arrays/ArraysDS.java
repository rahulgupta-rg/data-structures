package com.company.arrays;

import java.util.ArrayList;

public class ArraysDS {

    public ArrayList<Integer> getMaxAndSecondMax(int []a, int n){

     /*   int max ;
        int max2;
        for ( int i = 0 ; i < n ; i++ )
        {
            if(a[i] > max)
            {
                max2 = max ;
                max = a[i] ;
            }
            if(a[i] < max && max2 < a[i])
            max2 = a[i];
        }*/
     return null;
    }
}
